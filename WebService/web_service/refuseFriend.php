<?phprequire_once('./database/db.php');require_once('./model/friend.php');require_once('./control/valideToken.php');$user = getUserFromToken();if ($user !== false) {    if ($db->delete('friends', 'id_user1 = :id_user1 AND id_user2 = :id_user2', array("id_user1" => $parameters[":id"], "id_user2" => $user->getId()))) {        $json = array(            'error' => false,        );    }}echo json_encode($json);