<?php/* * Add contact   */require_once('../control/valideToken.php');$user = getUserFromToken();if ($user !== false) {    $contact = new Contact();    $contact->update($user->getId(), $parameters[":id"]);    $contact->setId(0);    $id = $db->insert($contact, 'contact');    if ($id != false) {        $contact->setId($id);        $json = array(            'error' => false,            'contact' => $contact->toDB()        );    }}echo json_encode($json);